server.modules += ( "mod_proxy" )

$HTTP["host"] == "skyjson.example.com" {
    proxy.server = (
        "" => (
            (
                "host" => "127.0.0.1",
                "port" => 8000
            )
        )
    )
}
